services:
  ai-agent:
    build: .
    container_name: ravi-agent_hindi_demo

    ports:
      - "8000:8000"

    env_file:
      - .env

    restart: unless-stopped

    command:
      - uvicorn
      - app.server:app
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --workers
      - "1"

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s