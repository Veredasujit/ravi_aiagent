# ── web / transport ──────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=13.0
aiohttp>=3.10.0
httpx>=0.27.0
python-dotenv>=1.0.1
pydantic>=2.8.0

# ── logging ──────────────────────────────────────────────────────────────────
loguru>=0.7.2

# ── audio + VAD ──────────────────────────────────────────────────────────────
numpy>=1.26.0,<2.3
silero-vad>=5.1
onnxruntime>=1.18.0
torch>=2.2.0          # silero-vad needs the tensor API even in onnx mode

# Optional: only if you want to use the official Teler SDK helpers.
# teler>=0.2.2
